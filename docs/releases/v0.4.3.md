# v0.4.3 - Evidence/preview fixes + observe docs

**Tag:** v0.4.3 · **Previous:** v0.4.2 · **Date:** 2026-07-21

## Summary

Patch: post-v0.4.2 fixes landed on main — evidence provider `written_path` /
observe bind rules and run artifact index, re-Fill signal and mirror
`data-state` honesty, run_status pointer, ts WARN; preview decision controls
gained distinct consequence descriptions. Docs: observe P1 mirror surface
honesty, RUN_ROOT, skill-predictability polish and capture≠judge decision
record.

## Changes

- **evidence:** `written_path` in capture result; observe bind rules; run
  artifact index (42a6590).
- **p2 fixes:** re-Fill signal, mirror `data-state`, `run_status` pointer,
  timestamp WARN (936662a).
- **preview:** distinct consequence descriptions on decision controls
  (f3045b1).
- **docs:** observe P1 mirror surface honesty + RUN_ROOT (bb4ccb2);
  skill-predictability polish + capture≠judge decision (a3f88bb).

## Validation

Changes shipped on main after v0.4.2 gates; no contract/seam changes —
capture ≠ judge boundary unchanged.
