# v0.9.0 — cross-run aggregate

Released 2026-08-01.

## New: run aggregate (`scripts/aggregate_runs.py`)

Deterministic cross-run seam over `.scratch/**/dogfood/*/` runs (CONTEXT: `run aggregate` / `repeat blocker`):

- Run discovery: default scan with `point-back.md` marker; `--runs` override.
- Per-run rollup: artifact completeness (plan / point-back / evidence manifest / preview / spec) + G5/G6 gate status via `validate_run.py`.
- Repeat-blocker detection: normalized `observed` text recurring across ≥2 runs — pure statistics, never a judgment; the "learning" is a frequency table, not prose.
- Output: JSON contract surface + `--md` thin view. Encoding fallback (UTF-8 → GB18030) for legacy dogfood runs.

Real scan on release: 17 runs, 88 pass / 3 blocked ledger rows, repeat blockers = 0 — no systemic defect pattern; the run seam is healthy.

## Architecture (2026-08-01 review, all landed)

- `mcp/preview/confirm.py` collapsed into owners (browser / transaction / server / util); digest lockstep test added (`tests/test_digest_lockstep.py`).
- Confirm-record parsing shared between G5 callers (`read_confirm_record` + `_g5_no_valid_reason` in `_preview_integrity.py`); `validate_run.py` back under its 800-line target (820 → 788).

## Validation

- 46 preview + 8 package + 56 repo-root tests green; `scripts/validate.py` VALIDATION PASSED; adapter floor self-check PASSED.
- Release gate: `python scripts/release.py` dry-run green.
