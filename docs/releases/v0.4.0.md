# v0.4.0 - Full-pipeline demo + observe evidence + button polish

**Tag:** v0.4.0 · **Previous:** v0.3.1 · **Date:** 2026-07-21

## Summary

Minor: full-pipeline demonstrability (preview* HITL + observe* evidence on a real product UI) + observe evidence extension + control-bar polish + infra. Decisions via wayfinder `v0.4-cycle` (4 themes, all resolved; speculative ones — P1 template-zone, launcher sunset — deferred per ADR-0010 rule 3 "vocabulary follows implementation").

## Changes

- **Theme 1 full-pipeline demo:** showcase gains preview* HITL demonstration (`04-preview-hitl.md` + `showcase/preview/`, sourced from dogfood 007); dogfood 007b (multi-step onboarding form, CJK) ran the full chain — preview* HITL → fill → craft → observe* (6 L6 evidence) → point-back verdict Pass.
- **Theme 2c observe extension:** evidence adapter `select_option` action (native `<select>` by value/label, fires change); G6 `observed` may carry commentary (validate_run + ui-evaluator + fixture).
- **Theme 3a forensic marker:** spec-template.md `<!-- spec-schema: 1 -->` (HTML comment; G1 doesn't read, forensic use; real schemaVersion GATE deferred per dx-05).
- **Theme 3c gate 1 semi-auto:** doctor gate1 structural smoke (GATE1_EXPECTED_* constants: 6 skills / 3 commands / namespace); dynamic `--plugin-dir` load + `/help` stay human (host slash not automatable).
- **Control-bar button polish (3 rounds, dogfood 007 findings):** d46682d (split pill annotate intent + abort two-step arm + i18n localize); 8b34387 (z-index float-note 999<1000 + mobile footer flex + abort drawer-click cancel); 4b370c5 (mobile ready a11y + footer order [Draft][Revise][Confirm]); round-2 follow-up (abort flex:0 0 auto + justify:flex-start + doctor gate1 constants).
- **test_release version-agnostic** (reads plugin.json version; no bit-rot on bump).

## Validation

FLOOR SELF-CHECK, SEAM TEST, FRONTEND FLOOR TEST (S1-**S16**, incl. S16 drawer-click cancels abort arm), `validate.py --strict`, `doctor`, `tests/` (release/doctor/run_status), evidence stdio 10/10 — all PASSED.

**Honest scope note:** button round-2/3 (8b34387 z-index/mobile/abort-cancel + 4b370c5 ready/footer) is covered by `test_floor_frontend` S1-S16; dogfood 007b verified round-1 (d46682d) full chain. Round-2/3 not re-dogfood'd — z-index visual + drawer-cancel are exercised by the automated floor test, not a second dogfood run.

## Deferred (v0.4.1+)

- Theme 2a/2b P1 template-zone (ADR-0010; no dogfood consumer yet, YAGNI).
- Theme 3b community catalog (gated on gate-5 install smoke).
- Theme 4 launcher sunset (v0.5 per dedup-05).
- Button LOW polish: i18n JS `data-*` injection (format-on-braces坑), dialog.show fallback, non-modal focus trap, abort-timeout broadcast.
