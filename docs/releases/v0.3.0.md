# v0.3.0 - Polish & v0.3-to-spec

**Tag:** v0.3.0 (pending) · **Previous:** v0.2.2 · **Date:** 2026-07-20

## Summary

This release focuses on perfecting the v0.3 scope based on Q3 grill decisions and DX feedback triage:

- **Preview control bar UX overhaul** (control.py):
  - Pill primary is now context-sensitive: direct submit (confirm) when readiness is met (substantive feedback or complete anchors), no forced drawer open.
  - Added "仅记录批注但暂不决定" draft button in drawer footer.
  - Label auto-switches to confirm text + `.is-direct-confirm` styling when ready.
  - Mobile: revise button no longer hidden.
  - A11y improvements (aria-labels, focus-visible, etc.).
  - Revise remains lenient (bypasses floor).
- **ADR-0008 floor enforcement** fully landed across sites + tests.
- **Test coverage**: Expanded `test_floor_frontend.py` with S10-S12 for new button behaviors (direct confirm, draft, label switch). All scenarios + floor pass.
- **Wayfinder & docs**: dx-feedback-triage closed with UI changes as 02 visibility polish. Elevate implement for 01-03 verified/landed in layout. v0.3-to-spec advanced and marked done.
- **Version bump** to 0.3.0, release gate prep.

All automated gates green. Perfect v0.3 polish for better HITL preview experience and install structure.

## Changes

- Preview button flows & draft (per UX debate).
- Test scenarios S10-12.
- Phase, maps, tracker updates for v0.3.
- Version to 0.3.0 in plugin/marketplace.

## Validation

- `test_validate_run.py`: SEAM TEST PASSED
- `test_floor_frontend.py`: FRONTEND FLOOR TEST PASSED (S1-S12)
- `scripts/validate.py`: VALIDATION PASSED
- py_compile / syntax checks passed.
- Dogfood runs validated flows.

## Next

v0.4 for deferred items (showcase expansion, third dogfood scene, etc.). Community catalog, etc.

See `docs/agents/product-workflow.md` and wayfinder maps for details.