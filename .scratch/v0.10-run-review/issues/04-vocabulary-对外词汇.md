# Ticket 04-vocabulary — 对外词汇与禁止事项

Type: decision
Status: resolved
Resolved: 2026-08-03 (roundtable synthesis)

对外能力名 `run review`；`run aggregate` 不外输（绑本仓脚本语义）。`repeat blocker` 原样保留，首现处一句定义（counting, not judging）。Never 取集中 `禁止:` 块四条：不新建 run ledger / 不散文化 lessons / 不自动回流 baseline / 不得按语义聚类 observed（只准逐字分组）。ledger 行格式不复述，pointer 指 ui-evaluator step 2。
