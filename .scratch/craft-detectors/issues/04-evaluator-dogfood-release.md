# 04 — Consume detector ledger and prove the craft loop

**What to build:** A complete Design I/O run carries an eight-row craft detector ledger into evaluation, preserves missing proof as a visible gap, maps real hits to declaration owners, and closes with documented local and Ubuntu release evidence.

**Blocked by:** 02 — Cover hierarchy and composition failures; 03 — Cover type, controls, motion, and baseline exceptions.

**Status:** ready-for-agent

- [ ] `ui-evaluator` consumes the craft ledger as supporting input and retains sole authority for declaration source, severity, and verdict.
- [ ] A `blocked` detector reaches evaluator as a craft proof gap; implemented UI cannot claim complete craft Pass from missing proof, while planning-only output can record explicit `N/A` rationale.
- [ ] Detector hits map through the existing recirculate authority before repair; detector protocol does not directly own edits.
- [ ] Real dogfood from rendered and source inputs emits exactly eight detector rows and demonstrates `hit`, `clear`, and degraded/blocked evidence behavior.
- [ ] Dogfood records evaluator consumption without placing craft ledger rows in the G6 manifest or L6 evidence ledger.
- [ ] Full static validation, run seam, package tests, frontend floor, release checks, and final Ubuntu CI pass.
- [ ] Decision map, spec, tickets, phase pointer, and any required ADR/reference docs match shipped authority and completion state.
