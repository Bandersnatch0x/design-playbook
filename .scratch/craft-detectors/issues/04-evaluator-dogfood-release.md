# 04 — Consume detector ledger and prove the craft loop

**What to build:** A complete Design I/O run carries an eight-row craft detector ledger into evaluation, preserves missing proof as a visible gap, maps real hits to declaration owners, and closes with documented local and Ubuntu release evidence.

**Blocked by:** 02 — Cover hierarchy and composition failures; 03 — Cover type, controls, motion, and baseline exceptions.

**Status:** resolved

- [x] `ui-evaluator` consumes the craft ledger as supporting input and retains sole authority for declaration source, severity, and verdict.
- [x] A `blocked` detector reaches evaluator as a craft proof gap; implemented UI cannot claim complete craft Pass from missing proof, while planning-only output can record explicit `N/A` rationale.
- [x] Detector hits map through the existing recirculate authority before repair; detector protocol does not directly own edits.
- [x] Real dogfood from rendered and source inputs emits exactly eight detector rows and demonstrates `hit`, `clear`, and degraded/blocked evidence behavior.
- [x] Dogfood records evaluator consumption without placing craft ledger rows in the G6 manifest or L6 evidence ledger.
- [x] Full static validation, run seam, package tests, frontend floor, release checks, and implementation Ubuntu CI pass.
- [x] Decision map, spec, tickets, phase pointer, and ADR-0014 match shipped authority and completion state.

## Answer

Preview control dogfood found one CRAFT-07 issue, mapped it to `components`, removed platform emoji, and closed with S19 plus desktop/mobile rendered facts. Initial CRAFT-05 keyword suspicion was correctly cleared after selector-scope exception review. Local 46 root tests, 60 package tests, SEAM, S1-S19 frontend, six Pin Bridge scenarios, and release gate passed. Implementation Ubuntu CI `30210795805` green.
