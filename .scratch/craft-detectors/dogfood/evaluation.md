# Evaluator consumption — Preview control after repair

## Bound declarations

- `craft`: detector protocol and craft-guard control-language rules
- `components`: stable control representation
- G6 manifest: absent by design; detector ledger is not runtime evidence

## Consumption

All eight final detector rows are `clear` with rendered/source evidence and exception checks. Evaluator retains declaration mapping, severity, and verdict authority.

## Closure

- closes: Annotate control uses a decorative platform emoji before its stable text label -> recirculate -> remove emoji span, retain localized Annotate text and count badge -> re-eval S19 + rendered/source facts -> 0 blocking

## Authority check

No detector row entered `evidence/manifest.jsonl` or an L6 evidence ledger. Before finding source/severity came from evaluator, not detector. This Pass covers craft-detector dogfood only; it does not claim product L6 acceptance.

## Verdict

**Pass.** Eight final detector rows clear after one owner-mapped repair; zero craft proof gaps remain.
