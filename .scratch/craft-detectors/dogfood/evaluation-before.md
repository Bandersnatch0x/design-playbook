# Evaluator consumption — Preview control before repair

## Bound declarations

- `craft`: detector protocol and craft-guard control-language rules
- `components`: familiar and stable control representation
- G6 manifest: absent by design; detector ledger is not runtime evidence

## Consumption

All eight detector rows were read. CRAFT-07 supplies advisory evidence only; evaluator maps it to `components`, assigns severity, and owns verdict.

## Point-back findings

issue:    Annotate control uses a decorative platform emoji before its stable text label
source:   components
fix:      remove the emoji span; retain explicit localized Annotate text and count badge
severity: low

## Authority check

No detector row entered `evidence/manifest.jsonl` or an L6 evidence ledger. Detector did not assign declaration source, severity, or verdict.

## Verdict

**Recirculate.** Return to the control primitive source, apply the positive fix, then rerun rendered/source detector review.
