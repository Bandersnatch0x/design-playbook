# Craft detector ledger — Preview control (before repair)

## Inputs

- Implemented surface: Preview decision control generated by `mcp/preview/control.py::_build_control()`.
- Rendered proof: desktop/mobile Chromium captures, `render-facts.json`, and `type-render-facts.json`.
- Behavior proof: frontend floor scenarios S1-S18 passed.
- Source proof: `packages/design-playbook/mcp/preview/control.py`.
- Verified project baseline: none for this isolated adapter surface.

## Detector ledger

| ID | Status | Rendered evidence | Source evidence | Exception check | Positive fix |
| --- | --- | --- | --- | --- | --- |
| CRAFT-01 | clear | Drawer exposes one emphasized final confirm action | Primary, secondary, and danger classes are distinct | Multiple outcomes are required, but only confirm is primary | - |
| CRAFT-02 | clear | Drawer is one focused tool, not repeated content cards | Chromium reports zero card-class elements | Repeated decision controls are actions, not browse objects | - |
| CRAFT-03 | clear | One drawer frames the decision tool over an unframed prototype | Source contains one dialog drawer and no nested Card primitive | Drawer is a genuine framed tool | - |
| CRAFT-04 | clear | Neutral surfaces carry area; teal readiness and red abort have separate roles | Tokens distinguish neutral, accent, accent-deep, and danger | Semantic roles remain distinct | - |
| CRAFT-05 | clear | Capsule shape is limited to the compact collapsed shell and status/count elements | Every `999px` control selector is scoped under `.dpb-pill`; drawer controls use restrained radius | Floating compact shell and status counts warrant capsule geometry | - |
| CRAFT-06 | clear | Desktop/mobile computed facts show maximum control text 16px and no text overflow | Source uses 13px base, 12.5px controls, and bounded title styles | Compact decision tool uses compact type roles | - |
| CRAFT-07 | hit | Annotate control is prefixed by a platform-rendered speech emoji | Source includes an aria-hidden `💬` span before stable Annotate text | Annotation command already has explicit text; decorative platform emoji adds no control meaning | Remove emoji span and retain explicit localized Annotate text plus count badge |
| CRAFT-08 | clear | S1-S18 interaction scenarios pass; reduced-motion media is active in Chromium facts | Short transitions explain state; armed pulses and pin flash are disabled under reduced motion | Decision arming legitimately needs state feedback | - |

## Calibration note

Initial pass suspected CRAFT-05 from raw `999px` counts. Selector-scope review showed those shapes belong only to the collapsed pill shell and compact status elements, so result was corrected to `clear` before evaluation. Detector protocol requires exception checking, not keyword counting.
