# Codex ~/.codex/config.toml example. Replace placeholders locally.
# Preferred path is the plugin-bundled runtime; the sibling launcher still works.
[mcp_servers.design-playbook-evidence]
command = "python"
args = ["<abs-path-to-repo>/packages/design-playbook/mcp/evidence/server.py"]
env = { DESIGN_PLAYBOOK_RUN_ROOT = "<abs-path-to-run-root>" }
