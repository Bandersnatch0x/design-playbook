# User-test session notes — L6.1 supplementary (quarantined)

Round 1 usability session, 2026-08-14. Facilitated weekly-report export
task with 3 operations participants (convenience sample).

Observed behavior: 2 of 3 participants first looked for the export entry
in the global toolbar, then used the inline batch export after a hint.

Status: **unusable as evidence** — the session ran before the consent /
anonymisation / retention record was completed, so the manifest entry
carries `method: user-test` with `population` but no `ethics` key. Per the
method-semantics gate this entry is quarantined as blocked evidence: it
cannot support any judgment (the L6.1 pass rests on
`L6.1-export-trace.json`, not on these notes). Complete the ethics record
in a newer manifest entry to make the session citable.
