<!-- run-profile: v1 -->

```yaml
tier: P3
criteria:
  - decided-fields: revises existing decided (l1.goal supersedes via D-0008) + adds l6.c4-c6
  - spec-touch: full six-layer draft with structural region re-composition (topbar counts -> global run console)
  - blocking: not bounded at intake
  - routes: R1 revisions + structural R2 + R3 re-entry legal
  - decision-tier: E (criterion 2 composition change; R3 re-entry re-graded E)
  - shaping: full S0-S6 session with T3 route registered
  - coverage: exhaustive full + sampling matrix fully executed (S6 machine face)
confirmed_by: user + 2026-08-14T09:05:00Z
skipped: []
upgrades: []
```

# 队列监控升级（全局模拟运行中心第一步）— plan

## 本次 run 范围

- L1：l1.goal 修订（D-0008 supersedes D-0001）——「全局模拟运行中心」第一步。
- L2：顶栏计数区升级为全局运行控制台 region（feed 跨页保持 + 全局暂停/恢复）。
- 范围外：场景编辑、完整决策审计（沿用 decided 非目标）。

## ui-picker 输入包

- scene hints: console-tight 运维监控主列表 + 全局运行 feed。
- constraints: 基线 DESIGN.md（sha256:8f4d2b61c7a3e95d0f8c2a1b6e9d4c7f3a5b8d1e0c2f4a6b8d0e2c4f6a8b0d2f 起，漂移后复核重绑）；顶栏不承载任务级数据的惯例由控制台 region 取代（基线漂移三出口处理）。
- exclusions: 不引入卡片墙；不做营销式 hero；控制台不做全量历史回放。
